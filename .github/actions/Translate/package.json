{
  "name": "translate-action",
  "version": "1.0.0",
  "private": true,
  "description": "Translates a BC .g.xlf against per-language .xlf files using the TRADOS Translation Lookup API",
  "scripts": {
    "build": "tsc && ncc build dist-tsc/index.js -o dist -m"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "axios": "^1.16.0",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/xml2js": "^0.4.14",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.9.3"
  }
}
